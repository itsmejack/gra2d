abstract class MapSize {
    String translation;
    int mapSizeX;
    int mapSizeY;
    int maxTime;
    private MenuState menu;

    MapSize(MenuState newMenu) {
        this.menu = newMenu;
    }
}
